##Head

###`_.head(array)`

-

###Arguments

array (Array): The array to query.

###Returns

Returns the first element of array.


###Example

```javascript
_.head([1, 2, 3]);
// => 1

_.head([]);
// => undefined
```
